# Desktop Cat — Enhanced Edition
# Install with:  pip install -r requirements.txt

# Required on Windows for taskbar-aware work-area detection
pywin32

# Recommended: pixel-art sprite scaling (nearest-neighbour) & system tray icon
Pillow>=10.0.0

# Optional: system tray icon with full menu
pystray>=0.19.0
